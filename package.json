{
  "devDependencies": {
    "@babel/core": "^7.12.10",
    "@babel/preset-env": "^7.12.11",
    "babel-jest": "^26.6.3",
    "jest": "^26.6.3"
  },
  "scripts": {
    "test": "jest",
    "watch": "jest --watch",
    "build": "babel src -d dist && cp index.html dist && cp style.css dist"
  },
  "dependencies": {
    "@babel/cli": "^7.12.10"
  }
}
